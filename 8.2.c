#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int num1, num2;
	num1 = 650;
	num2 = 250;
	if (num1 < num2)
	printf("\n so luong lon hon: %d", num1);
	else 
	printf("\n sp luong nho hon : %d", num2);

	
	return 0;
}
